<!-- Parent: ../../../CLAUDE.md -->
<!-- Scope: MCP tool surface — Tool registration in FastMCP + boundary validation -->

# tools — MCP Tool Surface

A thin layer that registers tools with FastMCP. For domain constraints (LakehouseModule query-only, etc.), see `modules/CLAUDE.md`, and for scenario engine internals, see `scenario/CLAUDE.md`.

## file

- `module_tools.py` — Register all module tools as `register_module_tools(mcp, stage, viewport, lakehouse, extension, simulation, process, robot, job, asset, character)`
- `scenario_tools.py` — Register scenario tool as `register_scenario_tools(mcp, config, stage, viewport, lakehouse, extension, simulation, robot, job, asset, character)`. All module arguments are required — if the runner wants to dispatch `module: ...` in scenario YAML, it must be passed here.
- `__init__.py` — re-export both

## Registration Terms and Conditions

```python
@mcp.tool()
async def stage_load_usd(usd_url: str, prim_path: str, ...) -> str:
    """Docstring is exposed as the MCP tool description; keep it concise and specific."""
    meta = make_meta(ModuleName.STAGE)
    request = {...}
    result = await simulation.stage_load_usd(meta, request)
    return _serialize(result)
```

- `@mcp.tool()` decorator — function name is tool name
- Create `OperationMeta` from `make_meta(ModuleName.X)`. Passed as the first argument to the module method
- The result is returned after converting to JSON string as `_serialize(result)`.
- Docstring = tool description displayed in MCP client UI (Claude Code, Codex CLI, etc.)

## Boundary Validation

`_resolve_safe_path()` of `scenario_tools.py` is responsible for path traversal defense of the scenario YAML path:

- Reject if the input path is not under `SCENARIOS_DIR` (environment variable, default `scenarios/`)
- Escape is not possible using absolute path/parent path reference (`..`)
- All scenario_* tools pass this function before receiving the YAML path.

## MCP Tools

**Source of truth**: `EXPECTED_MODULE_TOOLS` / `EXPECTED_SCENARIO_TOOLS` frozenset of `tests/unit/test_tools_registration.py` specifies the set of all tools. When adding a phase, only these two frozensets are updated → count assertion is automatically set to `len()`.Non-obvious constraints by group (see frozenset for full name):- **Process (`mcp_runtime_info` / `kit_app_*` / `process_list_kit_instances`)** — `mcp_runtime_info` is read-only MCP-server freshness diagnostics; call it before live result-shape validation after changing `src/omniverse_kit_mcp`, and restart the MCP host if expected tools/fields are absent or source files are newer than import time. `kit_app_*` controls only kit.exe lifecycle without extension. The default for live workers is to attach/start to an existing instance with `kit_app_start`, and `kit_app_restart` is used only when crash/hang is confirmed, validation_api changes itself, extension.toml dependency/native changes, `extension_reload` fails, and user fresh-process requests are made. `process_list_kit_instances` is a read-only enumerate — returns all non-MCP-spawned user GUI instances/even other MCP servers (differentiated by the `is_this_mcp_instance` flag). Used to check external instances before destructive work (Kit `user.config.json` edit / settings reset / extension force reload). Windows-only (PowerShell `Get-CimInstance`)
- **Stage READ/ASSERT (`stage_*`)** — `stage_diff_snapshots` is context-aware (receives the preceding two `stage_capture_snapshot` step ids). `stage_compute_world_bbox` is based on live USD BBoxCache, `stage_visual_alignment_report` quantifies visual/physics/acceptance volume misalignment with XY IoU + center delta of reference/candidate bbox, and `stage_placement_validation_report` is a broad-phase world-AABB placement gate for explicit PlacementZone/AcceptanceVolume prims; it does not replace final `viewport_capture` visual acceptance.
- **Stage WRITE (`stage_load_usd` / `stage_set_property` / `stage_set_semantic_label` / `stage_create_prim` / `stage_delete_prim`)** — tools layer is routed to `SimulationModule` (implementation location is not StageModule). `stage_create_prim(prim_type=...)` accepts not only Cube/Sphere but also Camera, USDLux (DistantLight/DomeLight/...)
- **Semantic label (`stage_set_semantic_label`)** — A hole where `sensor_set_annotator` only attached an annotator but could not perform prim labeling. `USDSemantics.LabelsAPI`(`semantics:labels:<label_type>`) + best-effort legacy `Semantics` schema are both authors (so that the annotator catches whichever schema it reads). Labels are inherited as a subtree — once in the reference prop's parent. Non-existent prim is 400. Verification of segmentation/bbox pickup operation is live.
- **Simulation (`simulation_*`)** — `play` response is reflected asynchronously (is_playing=false is possible)
- **Viewport (`viewport_capture` / `viewport_compare_ssim` / `viewport_set_active_camera` / `viewport_focus_prim`)** — Requires GUI mode. Capture has a fallback to detach failure (see Extension CLAUDE). `viewport_focus_prim` performs GUI F-key/Frame Selected equivalent operation with Kit viewport framing API first and bbox→lookat fallback. `viewport_frame_prims` calculates camera eye/target/up with prim bbox to reduce camera placement trial and error, `viewport_project_points` returns world point → normalized/pixel coordinates, and `viewport_capture_assert` quickly handles failure in black/blank capture with pixel stats.
- **Extension — lifecycle/state (`extension_trigger` / `extension_get_state` / `extension_activate`)** — 409 `ExtensionBusyError` when `trigger` is repeatedly called. `activate(ext_id)` is enabled with bare ext_id (`ExtensionManager.set_extension_enabled_immediate`), unknown ext_id is HTTP 400 → `EXTENSION_ACTIVATE_ERROR`
- **Extension — UI automation + log capture (`extension_get_ui_tree` / `extension_ui_invoke` / `extension_capture_logs` / `extension_clear_logs`)** — Requires GUI mode. `get_ui_tree` matches window substring + `omni.kit.ui_test` widget iterate (custom allow-list can be specified with `widget_types=` argument, default is Button/Label/StringField/CheckBox/ComboBox/Float*/Int*/ToolButton/RadioButton/Image/Frame/HStack/VStack/CollapsableFrame/ScrollingFrame/TreeView/Menu/MenuItem/Image/ImageWithProvider/Spacer/Separator). `ui_invoke` is click/double_click/type/select/check/uncheck, invalid path is HTTP 400. `clear_logs` starts request-scoped carb Console capture window and empties the ring buffer. For live failure verification, Warning/Error evidence is collected through `extension_clear_logs` right before the risky MCP call and `extension_capture_logs(level="WARN", stop_after_capture=True)` right after the failure. `capture_logs` is peek (not drain) and organizes the hook with `stop_after_capture=True`.
- **Window (`window_capture` / `window_list` / `window_ui_list` / `window_ui_show` / `window_menu_list` / `window_menu_trigger`)** — Kit GUI full screenshot + click menu + toggle `omni.ui.Window`. Requires GUI mode. `window_capture(wait_stable=True)` waits for async loading with continuous pixel diff polling (sha256 identity is useless due to FPS overlay). `window_menu_trigger` response includes `created_prims: [...]` difference list — check whether menu item actually created prim (silent no-op if empty array)
- **Navigation (`navigation_bake` / `navigation_query_path` / `navigation_add_exclude_volume`)** — NavMesh operations. `bake` requires **timeline stopped status** (`get_navmesh()` = None silent False Positive when called during playing) + non-blocking polling (`start_navmesh_baking()` kick + `is_navmesh_baking()` poll + `next_update_async()` yield — Kit maintains HTTP router responsiveness). Polling upper limit as `timeout_s` (default 300). `query_path` does the same polling auto-bake when NavMesh is unbaked (response `auto_baked: true`). `add_exclude_volume(prim_path=...)` automatically places Exclude based on bbox — for avoiding chair / low prop step-up artifact
- **Lakehouse (`lakehouse_query`)** — **query only** (no inject/cleanup)
- **Robot (`robot_*`)** — `get/set_joint_positions` gets HTTP 400 (silent no-op blocking) without articulation. `simulation.play → pause` warm-up required before joint I/O. `robot_navigate_to` is ASYNC (return job_id immediately). `robot_get_joint_config` sets_joint_positions symmetric readback — drive stiffness/damping/max_force + lower/upper limits + max_velocity per DOF. The `source` field is reported to the backend (`dof_properties` runtime view or `usd_drive_api` direct read fallback). `robot_get_joint_config_static` reads USD joint prim metadata without play for hazardous profile triage only; traversal order is not `set_joint_positions` write-order proof. IK / drive_physics used when debugging (drive too weak / target out-of-limit / velocity cap diagnosis)
- **Job (`job_status` / `job_cancel`)** — `job_status` is context-aware in the scenario (`navigate_step_id` or `job_id`). Robot/Character navigate all same polling path
- **Asset (`asset_list` / `asset_search` / `official_asset_*` / `external_asset_*`)** — `asset_list` is live: if category is not present, category list, if present, S3 directory listing (Isaac startup required, official URLs such as Franka are secured without hardcoding). `asset_search(query, category=None, limit=20)` is **offline** — Read `docs/assets/isaac/assets/*.md` curation catalog directly from MCP server process + ranking → `[{name, url, category, source_file}]` (REST / Isaac not required). Natural language → concrete USD URL (e.g. `asset_search("forklift")`), `AssetModule` (generator `catalog_dir` injection, lazy-load + in-memory cache). `official_asset_*` is the generated NVIDIA official browser-extension catalog from ignored `docs/references/official-assets/`: use `official_asset_search` before legacy `asset_search` for official providers/materials; stale hits may be returned with warnings but must go through `official_asset_verify` before use. Entry Workflow `docs/invariants/asset-discovery.md`. The markdown parser relies on the `tests/unit/test_asset_inventory_integrity.py` format convention ($VAR prefix·`Root:`·relative path). `external_asset_*` is prepare-only: use `asset_search` first, download/convert free external assets into ignored `.omniverse-kit-mcp/external_assets`, and do not treat converted local USDs as validated stage placement or `stage_load_usd(file://...)` substitutes.
- **File/Selection/Camera (`stage_save` / `stage_open` / `stage_new` / `stage_get_selection` / `stage_set_selection` / `viewport_set_active_camera`)** — GUI File menu + Stage panel + Viewport toolbar equivalent. `stage_open` also accepts https:// (Asset Browser URL) and automatically expands the tree to `stage_set_selection(expand_in_stage=True)`.
- **Character (`character_*`)** —
  - `character_load` is placed below `/World/Characters/<sanitized_name>` and applies 6.0 BehaviorAgent/IRA API. `sanitized_prim_path` in response is the actual USD placement path (`prim_path` in caller is echo — subsequent calls must be sanitized)
  - `character_play_animation.animation_name` is Extension Pydantic Literal `"Idle"|"Walk"|"Run"|"Sit"` — Other values are 422
  - `character_play_animation_variant(..., dispatch_mode="auto")` prefers Isaac Sim 6.0 BehaviorAgent task APIs when available and returns `behavior_task_*` readback; use `dispatch_mode="graph"` for legacy Action-variable-only writes, `"task"` to fail if no task API is available, or `"skel"` to directly bind a BuiltinActions `SkelAnimation` clip and seek to the annotation window for visual non-NavMesh playback.
  - `character_set_position` is USD write successful + character runtime override possible — For visual movement, use `character_navigate_to` or specify `position` when loading
  - `character_navigate_to` requires ASYNC + timeline playing (30s timeout after moving 0m for `simulation.stop/pause`)
  - `character_get_state` (prevents silent fake idle) when the extension response field is missing. `action` field is server-side cache based- **Scenario (`scenario_*`)** — MCP tools include 3 `scenario_validate` / `scenario_plan` / `scenario_last_report`. `scenario_list` / `scenario_schema` is not a tool, but MCP resource `isaacsim://scenarios` / `isaacsim://scenario-schema`. The path is `_resolve_safe_path()`, forcing SCENARIOS_DIR boundary- **Robot Advanced (`robot_list_arm_profiles` / `robot_probe_arm_profile` / `robot_probe_arm_profiles` / `robot_navigate_path` / `robot_gripper_control` / `robot_set_ee_target` / `robot_get_ee_pose` / `robot_run_franka_pick_place` / `robot_install_franka_pick_place_playback_demo` / `robot_install_pick_place_playback_demo` / `robot_reset_pick_place_demo` / `robot_get_pick_place_demo_status`)** — `robot_list_arm_profiles` is Isaac Sim 6.0 built-in arm support matrix and is an offline catalog-derived tool, so there is no REST endpoint; The asset URL is resolved in `docs/assets/isaac/assets/robots.md` catalog SoT. support_status is (`validated_pick_place` / `candidate_pick_place` / `ik_only` / `profile_only`), and candidate/IK/profile-only is prohibited from claiming that the pick/place was successful. `robot_probe_arm_profile` / `robot_probe_arm_profiles` check manipulation readiness (articulation, joints, safe nudge, gripper, IK, EE pose), but probe success does not promote pick/place status; `dynamic_checks=false` is load/articulation/static-metadata hazard triage and is not probe-level controllability. Single-profile probes default `timeout_s=90.0`; pass null only for deliberate unbounded diagnostics. Batch probes use `per_profile_timeout_s` to record slow/blocked profiles and `batch_timeout_s` to return partial matrix rows before the MCP caller timeout. `robot_install_pick_place_playback_demo(profile_name=...)` is a profile selector and only `validated_pick_place` profiles route to executable playback. Candidate, IK-only, and profile-only arms return `status="unsupported"` until family controller/gripper live proof exists. Immediately after installing the playback demo, check `object_fit_ok` / bbox / limit / measured of `robot_get_pick_place_demo_status`, and if false, stop before the play cycle and collect bbox·viewport·WARN/ERROR evidence. `navigate_path` returns ASYNC (`job_id` + `num_waypoints` + `duration_s` returns, 400 if timeline playing is not passed in Extension — see scenarios/smoke/robot_navigate_path.yaml). `gripper_control` action ∈ {open, close, set}; `set` is required (if not present, 400). `set_ee_target` skip-candidate only for Franka — non-Franka robot_description / Lula module non-import fails with 400 explicit message → Robust run by wrapping with `continueOnFailure: true` in scenarios. `robot_get_ee_pose` is currently EE link's USD world transform readback — a telemetry tool that checks how far the controller/ScriptNode actually moved the hand, not IK solve. `robot_run_franka_pick_place` is the Isaac Sim official Franka PickPlaceController/RMPflow/ParallelGripper route and does not use kinematic carry; Success is determined by controller done + object bbox lift + final distance, and if the object does not actually move to the gripper, it is failed by `ROBOT_FRANKA_PICK_PLACE_FAILED`. `robot_install_franka_pick_place_playback_demo` / `robot_reset_pick_place_demo` / `robot_get_pick_place_demo_status` installs, resets and observes the same official controller at the kit update tick, creates a persistent demo that is replayed with GUI Play / `simulation_play`, and checks the bbox lift/place values. The object/grid asset URL of the demo scene is resolved from catalog SoT and delivered to REST. The default hover height of the official controller is absolute world Z=0.3, so the table-top object is automatically corrected above the pick/place z by the wrapper and reported as `end_effector_initial_height_source`; If the bbox center is not the grasp point, specify `picking_position` / `end_effector_orientation`.
- **Robot IK target current behavior** — `robot_set_ee_target` uses shipped Lula robot descriptions surfaced by `robot_list_arm_profiles`; non-Franka profiles with descriptions can be probe candidates. Unsupported descriptions or missing configs must be recorded as explicit unsupported/error evidence, not collapsed into batch failure.
- **Robot known dynamic timeout policy** — `robot_list_arm_profiles` exposes `dynamic_probe_recommended_profiles`, `static_only_probe_recommended_profiles`, `recommended_probe_mode_by_profile`, `recommended_probe_mode_reasons`, and `known_dynamic_timeout_profiles` / reasons so callers can plan safe probes and batches. `robot_probe_arm_profiles(profile_names=[...])` runs exact profiles in order and records unknown names as row-level hard errors, which is preferred for profile-specific reruns over relying on status/family plus `limit`. `robot_probe_arm_profile(static_only_for_known_dynamic_timeouts=true)` and `robot_probe_arm_profiles(static_only_for_known_dynamic_timeouts=true)` are opt-in safety policies. They record profiles with durable live dynamic-timeout evidence as static-only hazard rows and must not be reported as joint control, probe-level controllability, or pick/place proof.
- **Robot known pick/place blockers** — `robot_list_arm_profiles` also exposes `known_pick_place_blocker_profiles` / reasons for profiles with durable live playback hazards, such as direct low-level adapter trials that time out or degrade Kit. `robot_install_pick_place_playback_demo` unsupported responses also expose `diagnostics.known_pick_place_blocker` / reason at the call site. These blocker rows do not negate probe-level MCP controllability; use them to plan or avoid risky pick/place proof loops.
- **Robot probe controllability classification** — `robot_probe_arm_profile` / `robot_probe_arm_profiles` return `mcp_controllability` and `mcp_controllability_reason`. Treat `dynamic_joint_control` as dynamic joint read/write proof, `dynamic_joint_read_only` as read-only evidence, `static_load_articulation_metadata` as static hazard-triage evidence only, and `blocked_*` as blocker evidence rather than MCP controllability. Probe rows also expose `probe_proves_pick_place=false`, `pick_place_validation_status`, and `pick_place_validation_reason` so callers do not mistake capability probing for pick/place validation. Batch probe results also expose summary counts/profile lists such as `mcp_controllability_counts`, `mcp_controllability_profiles`, `probe_capability_level_name_counts`, `probe_capability_level_name_profiles`, `pick_place_validation_status_counts`, `pick_place_validation_status_profiles`, `unsupported_capability_counts`, `timed_out_profiles`, `batch_timeout_profiles`, `batch_aborted_profiles`, `blocked_profiles`, `hard_failure_profiles`, `lifecycle_recovery_profiles`, `unsupported_capability_profiles`, `ik_target_failure_profiles`, and `known_dynamic_timeout_routed_profiles`; inspect them before summarizing broad matrix runs.
- **Robot playback status timeout** — During pick/place proof loops, call `robot_get_pick_place_demo_status(timeout_s=...)` with a timeout shorter than the proof-cycle budget. Inspect `diagnostics.playback_progress` for controller event counts, sampled object center, lift delta, distance-to-target, contact/approach window offset deltas, and bounded next-offset recommendations when debugging a stalled adapter. These recommendations are next-trial diagnostics only, not validation. `ROBOT_FRANKA_PICK_PLACE_DEMO_STATUS_TIMEOUT` is a non-proof timeout signal requiring lifecycle recovery, not validation.
- **Character enhancement (`character_play_animation_variant` / `character_load_crowd`)** — `play_animation_variant` determines base action (Sit/Walk/Run/Idle) with variant prefix + best-effort style request. Even if the style is not in the runtime, the base action is maintained → Check the actual set key in response.variables_set. `load_crowd` is count 1-100, layout ∈ {grid, line, random}. Individual character load failures are collected with `loaded[].error` and batch continues — success_count must be confirmed.
- **Sensor deepening (`sensor_attach_contact` / `sensor_attach_imu` / `sensor_set_annotator` / `sensor_lidar_get_point_cloud`)** — contact / imu Xform prim fallback in the absence of `isaacsim.sensors.experimental.physics` module (`backend` field reports the actual path). `set_annotator` has a fixed annotator name, membership — unknown is 400. Individual annotator attach failures are collected in `skipped` dict. `lidar_get_point_cloud` uses `get_data("generic-model-output")` of `sensor_attach_rtx_lidar` symmetric readback — 6.0 experimental RTX sensor wrapper first, and uses the legacy Replicator annotator path as a fallback. Supports two shapes: structured numpy (`x/y/z/intensity` fields) or polar (`azimuth/elevation/distance` → Cartesian transformation). Non-rtx_lidar prim is 400, `num_points=0` + `warning` when called before simulation.play (prevents silent empty cloud). Mark `max_points` cut with `truncated` flag. If the `backend` field is `fallback_noop:<exc>`, the `omni.replicator.core` import fails. See scenarios/smoke/sensor_{contact,imu,annotator_config}.yaml.
- **Physics readback (`physics_get_rigid_body_state`)** — `apply_rigid_body` symmetric readback. `source` field reports backend: `physx_runtime` is SingleRigidPrim live readout (simulation.play is accurate only after at least 1 tick), `usd_initial` is USD authored value (reflects pre-play velocity, mass/COM is always accurate). RigidBodyAPI not applied, prim is HTTP 400. `is_kinematic` / `is_enabled` reads USD directly, so it is always accurate.
- **Joint drive (`physics_set_joint_drive`)** — `physics_create_joint` only creates joints and fills holes that had no actuation means. Apply `USDPhysics.DriveAPI(drive_type)` to existing joint prim (`angular`=Revolute / `linear`=Prismatic) + target/velocity/stiffness/damping/max_force. The non-existent joint prim is 400. `max_force=None` maintains PhysX default (infinite) — Be careful that if you send it as 0, the drive will not power. Actual operation requires RigidBodyAPI + `physics_set_scene` + `simulation_play` (behavior verification is live).
- **Advanced Simulation (`simulation_step` / `simulation_step_observe` / `simulation_wait_until` / `simulation_set_time`)** — `step(frames)` gives priority to play-burst, which is the Isaac Sim 6.0 stable path, and if the timeline does not advance the actual time, set the target time with `set_time_fallback` (`advance_mode` field). `step_observe` collects and returns prim world pose / articulation joints / EE pose based on the same tick immediately after the step — reduces sleep + individual poll skew when debugging ScriptNode controller. `set_time` seeks immediately — maintains the current play state. `wait_until(until_time, timeout_s)` yields matic to `next_update_async` and waits until current_time is reached (deadlock-safe, replaces sleep+poll) — **timeline PLAYING required** (otherwise `timed_out=true`). Reply `reached`/`timed_out`/`elapsed_s`/`frames_waited`.
- **Replicator (`replicator_create_writer` / `replicator_register_randomizer` / `replicator_trigger_once` / `replicator_trigger_on_time`)** — writer_type ∈ {BasicWriter, KittiWriter, CocoWriter}. `trigger_once` takes priority over `rep.orchestrator.run_async(num_frames=N)` + sync `run` fallback. The kit schedule for `trigger_on_time` is `rep.trigger.on_time(interval_s)` wrap. Timeline play alone does not flush the writer — **must call trigger_once or on_time**. randomizer type ∈ {position, rotation, lighting}, target is a glob pattern, config is specifications for each type (position={volume}, rotation={min_rot,max_rot}, lighting={min_int,max_int}). When `omni.replicator.core` extension is not activated, backend=`fallback_noop:<ErrorType>` / `fallback_metadata:<ErrorType>`.
- **OmniGraph (`omnigraph_create_node` / `omnigraph_connect` / `omnigraph_execute` / `omnigraph_create_ros2_publisher` / `omnigraph_create_script_controller`)** — `create_node` automatically creates graph (execution evaluator) if it does not exist, and determines pre-existence with the `graph_existed` field. `execute` Manual tick — for deterministic execution when ActionGraph is waiting for a scene event. `create_ros2_publisher` is OnTick + IsaacCreateRenderProduct + ROS2PublishImage macro; The graph structure is created even without rclpy (response `ros2_available=false`, `nodes_created`/`edges_created` are returned as is → the skip signal is consumed by the scenario). `create_script_controller` performs OnPlaybackTick→ScriptNode wiring + `scriptPath` set + best-effort ScriptNode cache reset at once — MCP creates a scene/graph, and the actual Franka pick-place state machine is used for the pattern running inside the kit tick. If one of `omni.graph.action` + `isaacsim.ros2.bridge` is inactive, unknown type silently drops → Check the actual number of creations with the length of `nodes_created`.
- **Content (`content_browse` / `content_preview` / `content_inspect` / `content_resolve`)** — Asynchronize `list` / `stat` / `normalize_url` of `omni.client` with asyncio.to_thread. Entry shape: `{url, name, is_folder, size, modified_time_ns, flags}` (flags & 1<<4 this folder bit). If there is no login token in the Nucleus URL, report backend=`fallback_metadata:ClientLibraryError` (do not raise). S3 / https / file:/// is read-only without token. `omni.kit.window.content_browser` Kit extension does not require activation — `omni.client` is the Kit core.
- **Content geometry (`content_inspect`)** — `content_preview` is only file meta (size/mtime); `content_inspect` opens `USD.Stage.Open` as **worker thread(asyncio.to_thread)** and returns default_prim / world bbox(min,max) / meters_per_unit / up_axis / prim_count (main loop non-blocking — deadlock-safe, independent of context stage). The URLs that cannot be opened are `ok=false` + `backend=fallback:*`. bbox is best-effort (even if it fails, the meta is returned). Actual analysis requires Omniverse/HTTP resolver → Value calculation is live.
- **Kit Python runner (`kit_python_run`)** — Executes arbitrary Python source on Kit main thread. Tasks that the kit command registry cannot cover (USD relationship editing, `USD.EditContext` work, `omni.client` direct call, etc.) are called without GUI Script Editor paste. The response is `{ok, stdout, stderr, error, traceback, returned}` — returns as payload without throwing a script exception (caller checks `ok`). REST path `/commands/python_run` + service method `python_run` — Avoid open-paren pattern after `exec` literal in function/path name (security hook checks substring). Capture namespace variable with `return_keys`. user code assumes **localhost trusted** — no sandbox.- **Catalog (`extension_search`)** — optional local generated `docs/references/extensions.json` query. Since catalog is not committed in public clone, if there is no file, `EXTENSION_CATALOG_UNAVAILABLE` is returned. No REST/Isaac Sim process dependency. `keyword` is a substring match for name/title/summary/mcp_research_hint/raw_description/keywords, `app` is an isaacsim/usd_composer filter, and `category` is an exact match (case-insensitive). Results are {name, title, summary, category, apps, key_symbols, mcp_research_hint} per entry.
- **Advanced Extension Management (`extension_deactivate` / `extension_list_all` / `extension_get_info`)** — `deactivate` is idempotent to `set_extension_enabled_immediate(ext_id, False)` (`was_enabled=False` if already inactive). `list_all(enabled_only=False)` enumerates all Kit standard 621 ext + `{id, full_id, name, version, enabled, path, title}` summary. `get_info(ext_id)` matches iteration + bare id (Kit 107 returns None to bare dict) — Unregistered ext is 404. **Python module reimport is automatically handled by omni.ext.plugin (C++) fswatcher** (kit log: `FS Change triggers reloading: <path>` → `Processing ext disable request → on_shutdown → enable → on_startup`). `activate(reload=True)` only toggle — sys.modules cache is not cleaned up. Conclusion (updated 2026-05-26): Reliable `.py` reimport is **`extension_reload(ext_id)`** (sys.modules purge); fswatcher auto reload is unreliable on `_reload_enabled=False`. `extension.toml [dependencies]` / native dll / validation_api Only change yourself Kit restart. ⚠️ When reloading, 1 `ui.Window` zombie remains — `on_shutdown` standard cleanup required (`kkr-extensions/docs/lessons-learned.md` L9 correction + L16 reference).

## Related Boundaries

- **Brother CLAUDE.md**:
  - `../modules/CLAUDE.md` — Module-specific responsibility matrix + Character constraints + base.py pattern
  - `../modules/integration-facts.md` — 15 domain runtime trap (unnamed)
  - `../scenario/CLAUDE.md` — scenario engine (state_machine · action_registry · context-aware dispatch)
  - `../CLAUDE.md` (src/omniverse_kit_mcp/) — FastMCP package root · type boundary
- **Top**: root `CLAUDE.md` (Foundation · Change Ripple Matrix · Validation Rules)
- **Extension REST endpoint signature**: `../../../kkr-extensions/CLAUDE.md` (`rest_router.py` SoT + domain-specific unnamed point)
- **tool name SoT**: `../../../tests/unit/test_tools_registration.py` frozenset
- **Add resource**: `../mcp/resources.py` + `../../../tests/unit/test_resources_paths.py`
- Tool registration verification: `../../../tests/unit/test_tools_registration.py`
